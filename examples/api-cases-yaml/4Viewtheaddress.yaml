基础配置:  #后续测试报告中显示
  用例类型: ApiCase
  用例标题: 地址用例标题
  一级模块: 地址模块
  二级模块: 查看地址功能

前置脚本: ""

后置脚本: ""

用例步骤:
- 发送登录接口:
    操作类型: 发送请求POST
    请求地址: "{{URL}}"
    URL参数:
      s: api/useraddress/index
      application: app
      application_client_type: weixin
      token: "{{msg_token}}"

    请求数据: ""


- 提取响应数据data:
    操作类型: 提取数据JSON
    表达式: $.data.data[0].id
    变量名: dzid