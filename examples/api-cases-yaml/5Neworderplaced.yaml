基础配置:  #后续测试报告中显示
  用例类型: ApiCase
  用例标题: 订单用例标题
  一级模块: 订单模块
  二级模块: 提交订单功能

前置脚本: ""

后置脚本: ""

用例步骤:
- 发送登录接口:
    操作类型: 发送请求POST
    请求地址: "{{URL}}"
    URL参数:
      s: /api/buy/add
      application: app
      application_client_type: weixin
      token: "{{msg_token}}"

    请求数据:
      buy_type: "cart"
      ids: "{{isd_id}}"
      stock: "1"
      spec: []
      address_id: "{{dzid}}"
      payment_id: "3"
